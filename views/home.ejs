<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome to Lisper</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #1e3c72, #2a5298);
        color: #ffffff;
        text-align: center;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .container {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        padding: 50px 40px;
        border-radius: 15px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
        max-width: 600px;
        text-align: center;
      }

      .container h1 {
        font-size: 3.2rem;
        margin-bottom: 20px;
        letter-spacing: 1px;
      }

      .container p {
        font-size: 1.3rem;
        line-height: 1.6;
        margin-bottom: 35px;
        color: #e0e0e0;
      }

      button {
        padding: 14px 32px;
        font-size: 1.2rem;
        background-color: #00bfff;
        color: white;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(0, 191, 255, 0.4);
      }

      button:hover {
        background-color: #009acd;
        box-shadow: 0 6px 16px rgba(0, 191, 255, 0.6);
        transform: translateY(-2px);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Welcome to Lisper</h1>
      <p>
        Dive into a sea of secrets shared by strangers. Uncover stories you’ve
        never heard and connect with raw honesty. Your anonymous journey begins
        here.
      </p>
      <p>Refresh to see a new secret</p>
      <form action="/index" method="GET">
        <button type="submit">Listen to Secrets</button>
      </form>
    </div>
  </body>
</html>
